# 実装計画書 (Plan.md)

本ドキュメントは、小説家になろう（https://syosetu.com）のWeb小説をスクレイピングし、EPUB形式の電子書籍に変換・ビルドするPythonスクリプトおよびツールの開発計画です。
※本計画の策定にあたっては、`draft.md` および `sample/目次.html`、`sample/1.html` の構造を参考にしています。

---

## 1. 開発目的と概要
本ツールは、指定された「小説家になろう」の小説URL（または作品ID/Nコード）から、目次情報および各エピソード本文をスクレイピングして取得し、ローカルにHTML/テキストデータとして保存後、標準的なEPUB電子書籍フォーマット（EPUB 3形式推奨）に変換・結合して出力するPythonベースのコマンドラインツール（CLI）です。

---

## 2. 実装すべき機能詳細

### ① 環境構築と外部ライブラリのセットアップ (`setup.sh` または `requirements.txt`)
- 必要なサードパーティ製ライブラリの定義と自動インストールスクリプトの提供。
- 主要依存ライブラリ：
  - HTMLパース・スクレイピング：`beautifulsoup4`, `requests` (または `httpx`, `lxml`)
  - EPUB作成ライブラリ：`EpubMaker`（または自製EPUB圧縮、`ebooklib` などの実績あるライブラリ）
  - 文字コード・日本語処理等：`chardet` (必要に応じて)

### ② Webスクレイピングモジュール (`scraper.py`)
- **ターゲット構造の解析（`sample` フォルダ参照）**：
  - **目次ページ（`sample/目次.html`）**：
    - タイトル、作者名、前書き・あらすじ（`#novel_ex`）、章・エピソード一覧（`.p-eplist` 内の `.p-eplist__sublist`）および各エピソードURL（`/n9636x/1/` 等）の抽出。
  - **エピソードページ（`sample/1.html`）**：
    - サブタイトル（`.p-novel__title`）の取得。
    - 本文データ（`.js-novel-text` もしくは `.p-novel__body` 内の各段落 `<p>`）の抽出。ルビ（`<ruby>`, `<rt>`）の保持。
- **スクレイピング制御機能**：
  - スクレイピング先サーバーへの負荷低減のため、リクエスト間に必ずウェイト（秒単位のディレイ）を挿入する設定。
  - ネットワークエラー等のリトライハンドリング。
  - 取得したデータを一旦ローカルの作業用ディレクトリ（中間フォーマット）へ書き出すキャッシュ・保存機能。

### ③ 中間データ処理＆EPUB変換モジュール (`converter.py` / `epub_builder.py`)
- **中間HTMLのクレンジングと整形**：
  - 不要なナビゲーション、広告タグ、スクリプトの除去。
  - ルビ（`<ruby>`, `<rt>`）や縦書き表示に最適化されたHTML/XHTML構造への変換。
  - 表紙（カバーページ）、目次ページ、各エピソードページの作成。
- **EPUB構造体の構築とパッケージング**：
  - `mimetype` ファイルの作成。
  - `META-INF/container.xml` の生成。
  - `OEBPS/` ディレクトリ配下に、コンテンツ（XHTML）、画像（必要な場合）、スタイルシート（縦書き用CSS等）、およびメタデータ管理用 `content.opf`、ナビゲーション用 `toc.ncx` もしくは `nav.xhtml` を生成・配置。
  - ZIP圧縮によるEPUBファイル（拡張子 `.epub`）へのパッケージング（ZIPアーカイブの最初のファイルが非圧縮の `mimetype` である仕様を厳守）。

### ④ 実行制御メインスクリプト (`main.py`)
- ユーザーから対象小説のURL（例：`https://ncode.syosetu.com/n9636x/`）を受け取るコマンドライン引数の実装。
- スクレイピングから中間処理、EPUB生成までを一気通貫で実行できるワークフロー制御。

---

## 3. 開発スケジュールとマイルストーン

### フェーズ 1: 要件定義・基本設計・ライブラリ選定
- 目標：開発用パッケージリスト（`requirements.txt`）およびディレクトリ構成の確定。

### フェーズ 2: スクレイピング機能の開発
- 目標：指定されたNコードの目次からエピソードリストを取得し、各本文のテキストおよびHTMLをルビを保った状態でローカルに保存できること。

### フェーズ 3: EPUB変換・圧縮機能の開発
- 目標：ローカルの中間データを読み込み、適切なEPUBパッケージ（`.epub`）を生成・ビルドできること（EPUB検証ツール等でエラーが出ないこと）。

### フェーズ 4: 統合とCLIインターフェースの開発
- 目標：`main.py` からURLを指定して、一連の処理が完全自動で実行され、最終的なEPUBファイルが出力されること。

### フェーズ 5: テスト・デバッグ・リファクタリング
- 目標：サンプルHTMLや実際のなろうURL等を用いた正常系・異常系テストの実施。
- AGENTS.mdの指示に基づき、プログラムの各モジュールおよび関数ヘッダーに分かりやすい日本語のコメントを記述。

---

## 4. プログラム構成案

```text
syosetu-epub-maker/
├── Plan.md (本ドキュメント)
├── AGENTS.md (ルール定義)
├── requirements.txt (依存ライブラリ一覧)
├── main.py (実行エントリポイント)
├── src/
│   ├── __init__.py
│   ├── scraper.py (Webスクレイピング制御)
│   ├── converter.py (中間HTML整形・XHTML変換)
│   └── epub_builder.py (EPUBパッケージング・ZIP圧縮)
└── tests/
    ├── __init__.py
    ├── test_scraper.py
    └── test_epub_builder.py
```
